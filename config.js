const config = {
    user: "root",
    password: "root", // leave this blank for windows
    port:8889, // for windows and linex its 3306, 8889 for mac
    host: "localhost",
    database: "marvelcharacters",

}

module.exports = config;